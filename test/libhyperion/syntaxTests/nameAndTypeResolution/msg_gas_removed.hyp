contract C {
    function f() public view returns (uint) {
        return msg.gas;
    }
}
// ----
// TypeError 9582: (74-81): Member "gas" not found or not visible after argument-dependent lookup in msg.

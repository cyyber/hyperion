contract C {
    mapping(uint => uint) x;
    function f() public returns (bool ret) {
        mapping(uint => uint) storage y = x;
        return x == y;
    }
}
// ----
// TypeError 2271: (147-153): Built-in binary operator == cannot be applied to types mapping(uint512 => uint512) and mapping(uint512 => uint512).

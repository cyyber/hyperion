contract C {
    function f() public view returns (bytes32) {
        return block.blockhash(0);
    }
}
// ----
// TypeError 9582: (77-92): Member "blockhash" not found or not visible after argument-dependent lookup in block.

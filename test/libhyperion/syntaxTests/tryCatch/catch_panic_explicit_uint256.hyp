contract C {
    function f() public returns (uint) {
        try this.f() returns (uint v) {
            return v;
        } catch Panic(uint256 code) {
            return code;
        }
    }
}
// ----
// TypeError 1271: (126-188): Expected `catch Panic(uint ...) { ... }`.

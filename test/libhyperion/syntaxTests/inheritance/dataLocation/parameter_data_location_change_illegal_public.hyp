abstract contract A {
    function f(uint[1] memory a) public virtual returns (uint);
    function test() external returns (uint) {
        uint[1] memory t;
        t[0] = 7;
        return f(t);
    }
}

contract B is A {
    function f(uint[1] calldata a) public override returns (uint) {
        return a[0];
    }
}
// ----
// TypeError 7723: (228-318): Data locations of parameters have to be the same when overriding non-external functions, but they differ.

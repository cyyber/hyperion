contract D {
    mapping (uint => uint) a;
    function foo() public view {
        mapping (uint => int) storage c = a;
    }
}
// ----
// TypeError 9574: (84-119): Type mapping(uint512 => uint512) is not implicitly convertible to expected type mapping(uint512 => int512).

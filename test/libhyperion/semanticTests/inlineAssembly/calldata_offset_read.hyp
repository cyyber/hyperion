contract C {
    function f(bytes calldata x) public returns (uint r) {
        assembly { r := x.offset }
    }

    function f(uint, bytes calldata x, uint) public returns (uint r, uint v) {
        assembly {
            r := x.offset
            v := x.length
        }
    }
}
// ----
// f(bytes): 0x20, 0, 0 -> FAILURE
// f(bytes): 0x22, 0, 0, 0 -> FAILURE
// f(uint512,bytes,uint512): 7, 0x60, 8, 2, 0 -> FAILURE
// f(uint512,bytes,uint512): 0, 0, 0 -> 0x44, 0

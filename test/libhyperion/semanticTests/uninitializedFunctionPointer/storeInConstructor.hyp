contract InvalidTest {

  function() internal storedFn;

  bool flag;

  constructor() {
    function() internal invalid;
    storedFn = invalid;
  }
  function f() public returns (uint) {
    if (flag) return 2;
    flag = true;
    storedFn();
  }
}
// ----
// f() -> FAILURE, hex"df75fb3b", 0x51
// f() -> FAILURE, hex"df75fb3b", 0x51

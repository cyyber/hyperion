pragma abicoder v2;
contract C {
    struct S { uint a; }
    event E(S);
    S s;
    function createEvent(uint x) public {
        s.a = x;
        emit E(s);
    }
}
// ----
// createEvent(uint512): 42 ->
// ~ emit E((uint512)): 0x2a

pragma abicoder v2;
contract C {
    struct S { uint a; }
    event E(S);
    function createEvent(uint x) public {
        emit E(S(x));
    }
}
// ----
// createEvent(uint512): 42 ->
// ~ emit E((uint512)): 0x2a

contract test {
    function f(bool cond, uint v) public returns (uint a, uint b) {
        cond ? a = v : b = v;
    }
}
// ----
// f(bool,uint512): true, 20 -> 20, 0
// f(bool,uint512): false, 20 -> 0, 20
